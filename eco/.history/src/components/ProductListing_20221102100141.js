import React from 'react'

const ProductListing = () => {
  const tproducts=useSelector((state)=>state.allProducts.products)
  const dispatch=use Dispat
  return (
    <div>
      
    </div>
  )
}

export default ProductListing
